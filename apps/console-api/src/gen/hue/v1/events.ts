// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.3
//   protoc               unknown
// source: hue/v1/events.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Struct } from "../../google/protobuf/struct.js";
import { Timestamp } from "../../google/protobuf/timestamp.js";

export const protobufPackage = "hue.v1";

export interface Event {
  id: string;
  type: Event_Type;
  creationtime: Date | undefined;
  data: Event_Data[];
}

export enum Event_Type {
  TYPE_UNSPECIFIED = 0,
  TYPE_ADD = 1,
  TYPE_DELETE = 2,
  TYPE_ERROR = 3,
  TYPE_UPDATE = 4,
  UNRECOGNIZED = -1,
}

export function event_TypeFromJSON(object: any): Event_Type {
  switch (object) {
    case 0:
    case "TYPE_UNSPECIFIED":
      return Event_Type.TYPE_UNSPECIFIED;
    case 1:
    case "TYPE_ADD":
      return Event_Type.TYPE_ADD;
    case 2:
    case "TYPE_DELETE":
      return Event_Type.TYPE_DELETE;
    case 3:
    case "TYPE_ERROR":
      return Event_Type.TYPE_ERROR;
    case 4:
    case "TYPE_UPDATE":
      return Event_Type.TYPE_UPDATE;
    case -1:
    case "UNRECOGNIZED":
    default:
      return Event_Type.UNRECOGNIZED;
  }
}

export function event_TypeToJSON(object: Event_Type): string {
  switch (object) {
    case Event_Type.TYPE_UNSPECIFIED:
      return "TYPE_UNSPECIFIED";
    case Event_Type.TYPE_ADD:
      return "TYPE_ADD";
    case Event_Type.TYPE_DELETE:
      return "TYPE_DELETE";
    case Event_Type.TYPE_ERROR:
      return "TYPE_ERROR";
    case Event_Type.TYPE_UPDATE:
      return "TYPE_UPDATE";
    case Event_Type.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** Individual resource data */
export interface Event_Data {
  type: string;
  id: string;
  idV1?:
    | string
    | undefined;
  /** Keys the schema does not name individually. */
  additionalProperties?: { [key: string]: any } | undefined;
}

function createBaseEvent(): Event {
  return { id: "", type: 0, creationtime: undefined, data: [] };
}

export const Event: MessageFns<Event> = {
  encode(message: Event, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.type !== 0) {
      writer.uint32(16).int32(message.type);
    }
    if (message.creationtime !== undefined) {
      Timestamp.encode(toTimestamp(message.creationtime), writer.uint32(26).fork()).join();
    }
    for (const v of message.data) {
      Event_Data.encode(v!, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Event {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseEvent();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.id = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.type = reader.int32() as any;
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.creationtime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.data.push(Event_Data.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): Event {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      type: isSet(object.type) ? event_TypeFromJSON(object.type) : 0,
      creationtime: isSet(object.creationtime) ? fromJsonTimestamp(object.creationtime) : undefined,
      data: globalThis.Array.isArray(object?.data) ? object.data.map((e: any) => Event_Data.fromJSON(e)) : [],
    };
  },

  toJSON(message: Event): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.type !== 0) {
      obj.type = event_TypeToJSON(message.type);
    }
    if (message.creationtime !== undefined) {
      obj.creationtime = message.creationtime.toISOString();
    }
    if (message.data?.length) {
      obj.data = message.data.map((e) => Event_Data.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Event>, I>>(base?: I): Event {
    return Event.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Event>, I>>(object: I): Event {
    const message = createBaseEvent();
    message.id = object.id ?? "";
    message.type = object.type ?? 0;
    message.creationtime = object.creationtime ?? undefined;
    message.data = object.data?.map((e) => Event_Data.fromPartial(e)) || [];
    return message;
  },
};

function createBaseEvent_Data(): Event_Data {
  return { type: "", id: "", idV1: undefined, additionalProperties: undefined };
}

export const Event_Data: MessageFns<Event_Data> = {
  encode(message: Event_Data, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.type !== "") {
      writer.uint32(10).string(message.type);
    }
    if (message.id !== "") {
      writer.uint32(18).string(message.id);
    }
    if (message.idV1 !== undefined) {
      writer.uint32(26).string(message.idV1);
    }
    if (message.additionalProperties !== undefined) {
      Struct.encode(Struct.wrap(message.additionalProperties), writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Event_Data {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseEvent_Data();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.type = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.id = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.idV1 = reader.string();
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.additionalProperties = Struct.unwrap(Struct.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): Event_Data {
    return {
      type: isSet(object.type) ? globalThis.String(object.type) : "",
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      idV1: isSet(object.idV1)
        ? globalThis.String(object.idV1)
        : isSet(object.id_v1)
        ? globalThis.String(object.id_v1)
        : undefined,
      additionalProperties: isObject(object.additionalProperties)
        ? object.additionalProperties
        : isObject(object.additional_properties)
        ? object.additional_properties
        : undefined,
    };
  },

  toJSON(message: Event_Data): unknown {
    const obj: any = {};
    if (message.type !== "") {
      obj.type = message.type;
    }
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.idV1 !== undefined) {
      obj.idV1 = message.idV1;
    }
    if (message.additionalProperties !== undefined) {
      obj.additionalProperties = message.additionalProperties;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Event_Data>, I>>(base?: I): Event_Data {
    return Event_Data.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Event_Data>, I>>(object: I): Event_Data {
    const message = createBaseEvent_Data();
    message.type = object.type ?? "";
    message.id = object.id ?? "";
    message.idV1 = object.idV1 ?? undefined;
    message.additionalProperties = object.additionalProperties ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
