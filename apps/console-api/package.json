{
  "name": "@sharpn/console-api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The service the Console talks to, and the only thing that talks to the Gateway.",
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "start": "node dist/server.js",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "test": "vitest run",
    "hash-password": "node scripts/hash-password.mjs"
  },
  "dependencies": {
    "@bufbuild/protobuf": "^2.14.1",
    "@grpc/grpc-js": "^1.14.4",
    "@node-rs/argon2": "^2.2.1",
    "express": "^5.2.1",
    "zod": "^4.6.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^24.3.0",
    "typescript": "^5.9.3",
    "vitest": "^5.0.0"
  }
}
