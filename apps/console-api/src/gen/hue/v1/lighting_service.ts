// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.3
//   protoc               unknown
// source: hue/v1/lighting_service.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientUnaryCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";
import { Error, ResourceIdentifier } from "./common.js";
import { LightGet, LightPut } from "./lighting.js";

export const protobufPackage = "hue.v1";

/**
 * Deliberately empty. Hue serves the whole collection in one response and
 * offers nothing to filter or page by; the message exists so that adding
 * either later is a field rather than a new method.
 */
export interface ListLightsRequest {
}

export interface ListLightsResponse {
  lights: LightGet[];
}

export interface GetLightRequest {
  /** The Resource id, as `LightGet.id` reports it. */
  lightId: string;
}

export interface UpdateLightRequest {
  lightId: string;
  /**
   * What to change. Presence is the whole contract: a field left unset is
   * absent from the request the Bridge receives, and an unset field is never
   * the same as a zero one. `LightPut.on` unset leaves the light alone;
   * `on { on: false }` turns it off.
   */
  command: LightPut | undefined;
}

/**
 * What a mutation did, which is not always all or nothing: the Bridge can
 * report both a change it made and an error about the same request, in an
 * exchange it answered with HTTP 200. Collapsing that into a gRPC status
 * would throw half the answer away, so both halves are here.
 */
export interface MutationResponse {
  /** The Resources the Bridge says it changed. */
  updated: ResourceIdentifier[];
  /**
   * What the Bridge refused, in its own words. `Error` is generated from the
   * spec, so a Bridge that reports more than a description reaches clients
   * once the spec says it can — rather than being flattened here.
   */
  errors: Error[];
}

function createBaseListLightsRequest(): ListLightsRequest {
  return {};
}

export const ListLightsRequest: MessageFns<ListLightsRequest> = {
  encode(_: ListLightsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListLightsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseListLightsRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(_: any): ListLightsRequest {
    return {};
  },

  toJSON(_: ListLightsRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<ListLightsRequest>, I>>(base?: I): ListLightsRequest {
    return ListLightsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListLightsRequest>, I>>(_: I): ListLightsRequest {
    const message = createBaseListLightsRequest();
    return message;
  },
};

function createBaseListLightsResponse(): ListLightsResponse {
  return { lights: [] };
}

export const ListLightsResponse: MessageFns<ListLightsResponse> = {
  encode(message: ListLightsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.lights) {
      LightGet.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListLightsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseListLightsResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.lights.push(LightGet.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): ListLightsResponse {
    return {
      lights: globalThis.Array.isArray(object?.lights) ? object.lights.map((e: any) => LightGet.fromJSON(e)) : [],
    };
  },

  toJSON(message: ListLightsResponse): unknown {
    const obj: any = {};
    if (message.lights?.length) {
      obj.lights = message.lights.map((e) => LightGet.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListLightsResponse>, I>>(base?: I): ListLightsResponse {
    return ListLightsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListLightsResponse>, I>>(object: I): ListLightsResponse {
    const message = createBaseListLightsResponse();
    message.lights = object.lights?.map((e) => LightGet.fromPartial(e)) || [];
    return message;
  },
};

function createBaseGetLightRequest(): GetLightRequest {
  return { lightId: "" };
}

export const GetLightRequest: MessageFns<GetLightRequest> = {
  encode(message: GetLightRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.lightId !== "") {
      writer.uint32(10).string(message.lightId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetLightRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGetLightRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.lightId = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): GetLightRequest {
    return {
      lightId: isSet(object.lightId)
        ? globalThis.String(object.lightId)
        : isSet(object.light_id)
        ? globalThis.String(object.light_id)
        : "",
    };
  },

  toJSON(message: GetLightRequest): unknown {
    const obj: any = {};
    if (message.lightId !== "") {
      obj.lightId = message.lightId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetLightRequest>, I>>(base?: I): GetLightRequest {
    return GetLightRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetLightRequest>, I>>(object: I): GetLightRequest {
    const message = createBaseGetLightRequest();
    message.lightId = object.lightId ?? "";
    return message;
  },
};

function createBaseUpdateLightRequest(): UpdateLightRequest {
  return { lightId: "", command: undefined };
}

export const UpdateLightRequest: MessageFns<UpdateLightRequest> = {
  encode(message: UpdateLightRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.lightId !== "") {
      writer.uint32(10).string(message.lightId);
    }
    if (message.command !== undefined) {
      LightPut.encode(message.command, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateLightRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseUpdateLightRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.lightId = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.command = LightPut.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): UpdateLightRequest {
    return {
      lightId: isSet(object.lightId)
        ? globalThis.String(object.lightId)
        : isSet(object.light_id)
        ? globalThis.String(object.light_id)
        : "",
      command: isSet(object.command) ? LightPut.fromJSON(object.command) : undefined,
    };
  },

  toJSON(message: UpdateLightRequest): unknown {
    const obj: any = {};
    if (message.lightId !== "") {
      obj.lightId = message.lightId;
    }
    if (message.command !== undefined) {
      obj.command = LightPut.toJSON(message.command);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateLightRequest>, I>>(base?: I): UpdateLightRequest {
    return UpdateLightRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateLightRequest>, I>>(object: I): UpdateLightRequest {
    const message = createBaseUpdateLightRequest();
    message.lightId = object.lightId ?? "";
    message.command = (object.command !== undefined && object.command !== null)
      ? LightPut.fromPartial(object.command)
      : undefined;
    return message;
  },
};

function createBaseMutationResponse(): MutationResponse {
  return { updated: [], errors: [] };
}

export const MutationResponse: MessageFns<MutationResponse> = {
  encode(message: MutationResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.updated) {
      ResourceIdentifier.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.errors) {
      Error.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MutationResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseMutationResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.updated.push(ResourceIdentifier.decode(reader, reader.uint32()));
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.errors.push(Error.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): MutationResponse {
    return {
      updated: globalThis.Array.isArray(object?.updated)
        ? object.updated.map((e: any) => ResourceIdentifier.fromJSON(e))
        : [],
      errors: globalThis.Array.isArray(object?.errors) ? object.errors.map((e: any) => Error.fromJSON(e)) : [],
    };
  },

  toJSON(message: MutationResponse): unknown {
    const obj: any = {};
    if (message.updated?.length) {
      obj.updated = message.updated.map((e) => ResourceIdentifier.toJSON(e));
    }
    if (message.errors?.length) {
      obj.errors = message.errors.map((e) => Error.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MutationResponse>, I>>(base?: I): MutationResponse {
    return MutationResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MutationResponse>, I>>(object: I): MutationResponse {
    const message = createBaseMutationResponse();
    message.updated = object.updated?.map((e) => ResourceIdentifier.fromPartial(e)) || [];
    message.errors = object.errors?.map((e) => Error.fromPartial(e)) || [];
    return message;
  },
};

/**
 * The lights on the Bridge this Gateway is paired with.
 *
 * No request carries a Bridge ID: one Gateway serves one Bridge, which is the
 * supported subset. Adding more Bridges later adds a field rather than
 * changing one, so nothing here has to be rearranged for it.
 */
export type LightingServiceService = typeof LightingServiceService;
export const LightingServiceService = {
  /** Every light the Bridge knows about. */
  listLights: {
    path: "/hue.v1.LightingService/ListLights" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ListLightsRequest): Buffer => Buffer.from(ListLightsRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ListLightsRequest => ListLightsRequest.decode(value),
    responseSerialize: (value: ListLightsResponse): Buffer => Buffer.from(ListLightsResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ListLightsResponse => ListLightsResponse.decode(value),
  },
  /** One light by its Resource id. */
  getLight: {
    path: "/hue.v1.LightingService/GetLight" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: GetLightRequest): Buffer => Buffer.from(GetLightRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GetLightRequest => GetLightRequest.decode(value),
    responseSerialize: (value: LightGet): Buffer => Buffer.from(LightGet.encode(value).finish()),
    responseDeserialize: (value: Buffer): LightGet => LightGet.decode(value),
  },
  /**
   * Change one light. Only the fields set in `command` are sent to the
   * Bridge, so an unset `dimming` leaves the brightness where it was.
   */
  updateLight: {
    path: "/hue.v1.LightingService/UpdateLight" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: UpdateLightRequest): Buffer => Buffer.from(UpdateLightRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): UpdateLightRequest => UpdateLightRequest.decode(value),
    responseSerialize: (value: MutationResponse): Buffer => Buffer.from(MutationResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): MutationResponse => MutationResponse.decode(value),
  },
} as const;

export interface LightingServiceServer extends UntypedServiceImplementation {
  /** Every light the Bridge knows about. */
  listLights: handleUnaryCall<ListLightsRequest, ListLightsResponse>;
  /** One light by its Resource id. */
  getLight: handleUnaryCall<GetLightRequest, LightGet>;
  /**
   * Change one light. Only the fields set in `command` are sent to the
   * Bridge, so an unset `dimming` leaves the brightness where it was.
   */
  updateLight: handleUnaryCall<UpdateLightRequest, MutationResponse>;
}

export interface LightingServiceClient extends Client {
  /** Every light the Bridge knows about. */
  listLights(
    request: ListLightsRequest,
    callback: (error: ServiceError | null, response: ListLightsResponse) => void,
  ): ClientUnaryCall;
  listLights(
    request: ListLightsRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ListLightsResponse) => void,
  ): ClientUnaryCall;
  listLights(
    request: ListLightsRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ListLightsResponse) => void,
  ): ClientUnaryCall;
  /** One light by its Resource id. */
  getLight(
    request: GetLightRequest,
    callback: (error: ServiceError | null, response: LightGet) => void,
  ): ClientUnaryCall;
  getLight(
    request: GetLightRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: LightGet) => void,
  ): ClientUnaryCall;
  getLight(
    request: GetLightRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: LightGet) => void,
  ): ClientUnaryCall;
  /**
   * Change one light. Only the fields set in `command` are sent to the
   * Bridge, so an unset `dimming` leaves the brightness where it was.
   */
  updateLight(
    request: UpdateLightRequest,
    callback: (error: ServiceError | null, response: MutationResponse) => void,
  ): ClientUnaryCall;
  updateLight(
    request: UpdateLightRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: MutationResponse) => void,
  ): ClientUnaryCall;
  updateLight(
    request: UpdateLightRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: MutationResponse) => void,
  ): ClientUnaryCall;
}

export const LightingServiceClient = makeGenericClientConstructor(
  LightingServiceService,
  "hue.v1.LightingService",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): LightingServiceClient;
  service: typeof LightingServiceService;
  serviceName: string;
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
